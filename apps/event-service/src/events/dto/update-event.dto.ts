// UpdateEventDto is defined alongside CreateEventDto via PartialType — this
// file just re-exports it so consumers can import from a consistent path.
export { UpdateEventDto } from './create-event.dto';
